<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Profile Section -->
    <group android:id="@+id/group_profile" android:checkableBehavior="none">
        <item
            android:id="@+id/nav_profile"
            android:icon="@drawable/ic_profile_foreground"
            android:title="Profile" />
    </group>

    <!-- Main Navigation Items -->
    <group android:id="@+id/group_main_navigation" android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home_foreground"
            android:title="Home" />
        <item
            android:id="@+id/nav_notifications"
            android:icon="@drawable/ic_notification_foreground"
            android:title="Notifications" />
    </group>
</menu>
